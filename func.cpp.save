#include "func.h"
#include "story.h"
#include <iostream>

void PrintThis (std::string a)
{
std::cout<<a;
}

class Room
	{
	bool visited, destroyed, solved;
	std::string stuff [50];
	public:
	Room (bool, bool, bool);
	std::string stuffhere (stuff), description ();
	}
Room::Room (bool v, bool s, bool d)
	{
	visited = v;
	solved = s;
	destroyed = d; 
	}
